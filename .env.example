# Postgres (required for LIVE dashboard APIs)
DATABASE_URL=

# Supabase Auth (Phase A) — omit until migrated; auth screens show configuration required
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Optional integrations / payments
X402_FACILITATOR_URL=
STRIPE_SECRET_KEY=
BLOB_READ_WRITE_TOKEN=
